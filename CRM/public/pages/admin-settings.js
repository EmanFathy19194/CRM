import { escapeHtml, renderProtectedShell } from "./shared.js";
export async function renderAdminSettings() { renderProtectedShell(`<div id="page-content">Loading settings...</div>`, "/admin/settings"); const c = document.querySelector("#page-content"), [s, a] = await Promise.all([fetch("/api/admin/settings", { credentials: "same-origin" }).then(r => r.json()), fetch("/api/admin/audit-logs", { credentials: "same-origin" }).then(r => r.json())]); c.innerHTML = `<h1>Settings</h1><form id="settings" class="rule-form"><label>Organization<input name="organizationName" value="${escapeHtml(s.organizationName)}" /></label><label>Support email<input name="supportEmail" value="${escapeHtml(s.supportEmail)}" /></label><label>Default priority<select name="defaultTicketPriority">${["low", "medium", "high", "urgent"].map(x => `<option ${x === s.defaultTicketPriority ? "selected" : ""}>${x}</option>`).join("")}</select></label><button>Save</button></form><h2>Audit log</h2><section class="rule-table">${a.items.map((x) => `<article><strong>${escapeHtml(x.action)}</strong><small>${escapeHtml(x.actorEmail)} · ${escapeHtml(x.createdAt)}</small></article>`).join("") || "<p>No audit entries.</p>"}</section>`; c.querySelector("#settings").addEventListener("submit", async (e) => { e.preventDefault(); await fetch("/api/admin/settings", { method: "PATCH", credentials: "same-origin", headers: { "Content-Type": "application/json" }, body: JSON.stringify(Object.fromEntries(new FormData(e.currentTarget).entries())) }); void renderAdminSettings(); }); }
